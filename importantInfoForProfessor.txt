Hi professor, I just wanted to let you know that I reformatted the animalNames.txt file a little bit in 
order for my program to run correctly, the only thing I did was delete the empty lines that were in between
every line, that is because my program scans the names line by line, so if it scans an empty line then 
some values for the animals will just be an empty string instead of the actual value :)